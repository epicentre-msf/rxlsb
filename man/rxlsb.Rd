% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rxlsb.R
\name{rxlsb}
\alias{rxlsb}
\title{Read a sheet from an '.xlsb' workbook}
\usage{
rxlsb(path, sheet = 1, col_names = TRUE, col_types = NULL, na = "")
}
\arguments{
\item{path}{Path to the \code{.xlsb} file.}

\item{sheet}{Sheet to read. Either a sheet name (string) or a positive
integer giving its position in the workbook. Defaults to the first sheet.}

\item{col_names}{\code{TRUE} to use the first row as column names, \code{FALSE} to
generate names (\code{...1}, \code{...2}, ...), or a character vector of names to use.}

\item{col_types}{\code{NULL} to guess the type of each column (the default), or a
character vector of \code{"guess"}, \code{"text"}, \code{"numeric"}, \code{"date"},
\code{"logical"}, or \code{"skip"}. Recycled if length 1; otherwise must have one
entry per column. \code{"skip"} drops the column from the result.}

\item{na}{Character vector of strings to treat as missing values, in
addition to empty cells. Defaults to \code{""}.}
}
\value{
A \link[tibble:tibble]{tibble} with one column per column of the sheet.
}
\description{
Reads a single worksheet from an Excel binary (\code{.xlsb}) workbook into a
tibble. This is the \code{.xlsb} counterpart to \code{\link[readxl:read_excel]{readxl::read_xlsx()}}.
}
\details{
Cell types are taken from python-calamine: text becomes \code{character}, numbers
become \code{double}, and dates become \link{Date}. A column whose non-empty cells are
all of one type gets that type; a column with mixed types falls back to
\code{character}. Empty cells (and any string in \code{na}) become \code{NA}. Use
\code{col_types} to override this per column.

The Python package \code{python-calamine} is installed automatically (via
\code{reticulate}) the first time you read a file.
}
\note{
Cells are already parsed by python-calamine before \code{col_types} is applied,
so \code{"text"} is only fully faithful for cells Excel stored as text. For
cells stored as numbers, the original representation is already lost by the
time R sees them: dates coerced to \code{"text"} become an ISO string such as
\code{"2020-01-01"} (not the Excel serial number \code{readxl} returns, and not the
original display string), and a long numeric ID or code has already lost
any precision beyond what a double can hold.
}
\examples{
\dontrun{
rxlsb("workbook.xlsb")
rxlsb("workbook.xlsb", sheet = "data")
rxlsb("workbook.xlsb", col_names = FALSE)
rxlsb("workbook.xlsb", col_types = "text")
}

}
\seealso{
\code{\link[=list_sheets]{list_sheets()}} to list the sheets in a workbook.
}
